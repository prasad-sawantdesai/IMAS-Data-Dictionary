version: 2

build:
  os: "ubuntu-22.04"
  apt_packages:
      - openjdk-21-jdk
  tools:
    python: "3.11"
  jobs:
    post_checkout:
      - git fetch --unshallow || true
    post_create_environment:
      - mkdir -p ./saxonhe/
      - echo "Downloading Saxon-HE jar"
      - wget -O ./saxonhe/saxon-he.jar "https://repo1.maven.org/maven2/net/sf/saxon/Saxon-HE/12.4/Saxon-HE-12.4.jar"
      - echo "export CLASSPATH=\$CLASSPATH:$(pwd)/saxonhe/saxon-he.jar" >> ~/.bashrc
      - source ~/.bashrc
      - echo "Listing files in saxonhe folder"
      - echo "$CLASSPATH"
      - ls -l ./saxonhe/

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
      
sphinx:
  builder: html
  configuration: docs/conf.py
  fail_on_warning: false
